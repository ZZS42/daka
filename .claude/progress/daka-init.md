# DaKa 项目初始化

**任务**: 从 staffbridge 拆分工时记录模块为独立项目 DaKa，给 Mugi Ramen 用
**开始时间**: 2026-02-15
**最后更新**: 2026-02-15

## 当前状态
- 🟡 进行中 — MVP 已部署，待加 PIN 打卡模式

## 任务清单
- [x] Phase 0-3: 项目创建 + 代码搬迁 + 元反思
- [x] 分享功能：Web Share API + 工资汇总格式化文字分享
- [x] CLAUDE.md + Git + GitHub（ZZS42/daka）+ Vercel 部署
- [x] 部署 → https://daka-mugi.vercel.app
- [x] Vercel 连接 GitHub 自动部署（用户已后台授权）
- [x] 元反思：lint fix（useState lazy init）+ 空思维 + Explore 证据收集
- [x] 时间编辑功能：打卡后可修改上班/下班时间（忘打卡场景）
- [ ] **PIN 打卡模式**（员工自助打卡，不用找老板）
- [ ] 生成 PWA 图标（192x192 + 512x512）
- [ ] 手机实测 → 给丈母娘试用

## 上下文快照

### 关键决策
1-8 同前
9. **时间编辑** — today-tab 显示打卡时间，点击铅笔图标编辑（type="time" input）
10. **PIN 打卡模式** — 每个员工 4 位 PIN，手机放柜台，员工输 PIN 自动上班/下班
11. **域名** — daka-mugi.vercel.app（daka.vercel.app 已被占）

### 关键发现
- 元反思发现 lint 报错 `set-state-in-effect` — 改为 useState lazy init
- 元反思 P3：hydration mismatch（回访用户），Phase 2 用 useSyncExternalStore 解决
- 元反思 P3：`t.share` key 未使用（可做 aria-label）
- 空思维：URL Fragment 数据传递是好的 Phase 2 方向

### 文件结构
```
daka/
├── src/app/
│   ├── page.tsx          — 主页（状态+Tabs+3个Dialog）
│   ├── layout.tsx        — LocaleProvider
│   └── manifest.ts       — PWA manifest
├── src/components/
│   ├── today-tab.tsx     — 今日打卡（显示时间+编辑入口）
│   ├── employees-tab.tsx — 员工管理
│   └── summary-tab.tsx   — 工资汇总+分享
├── src/lib/
│   ├── timesheet-store.ts — 数据层（含 updateEntry）
│   └── i18n/              — 中英翻译
└── CLAUDE.md
```

### PIN 打卡模式设计（待实现）
- **store**: Employee 加 `pin: string` 字段 + `findByPin(pin)` 查询
- **新组件 pin-pad.tsx**: 大数字键盘（0-9）+ PIN 显示 + 清除
- **page.tsx**: header 加"打卡模式"切换按钮，进入全屏 PIN 界面
- **流程**: 输入 4 位 PIN → 自动识别员工 → 自动上班/下班 → 显示结果 3 秒 → 清空
- **员工管理**: 添加/编辑员工时设置 PIN

## 下一步
恢复后应该从这里开始：**PIN 打卡模式开发**。
1. timesheet-store.ts: Employee 加 `pin` 字段 + `findByPin()` + 迁移旧数据（无 pin 的员工）
2. translations.ts: 加 PIN 相关翻译（pinCode/enterPin/pinMode/clockSuccess/wrongPin/etc）
3. 新建 src/components/pin-pad.tsx: 数字键盘 + 自动触发
4. page.tsx: header 加切换按钮 + PIN 模式全屏
5. employees-tab.tsx: 添加/编辑员工时加 PIN 输入
6. 构建验证 + 元反思
7. git push（Vercel 自动部署）
